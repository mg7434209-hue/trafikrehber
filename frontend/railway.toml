[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
# SPA'yı bot görünürlüğü için head enjeksiyonuyla servis eder (bkz. server.js)
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
